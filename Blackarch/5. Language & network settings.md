# Start off topic
If the system does not start normally

    fdisk -l
    vim /etc/fstab
Adjusting the mount scheme is the same as `grub`

------------------------------------------------
## Language settings
